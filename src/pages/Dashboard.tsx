import { Header, Layout, MapComponent } from "@/components";

function Dashboard() {
  return (
    <Layout>
      <Header>Dashboard</Header>
      <MapComponent />
    </Layout>
  );
}

export default Dashboard;
