import { Header, Layout, MapComponent } from "@/components";

function Dashboard() {
  return (
    <Layout>
      <Header>RESPECTS</Header>
      <MapComponent />
    </Layout>
  );
}

export default Dashboard;
